---
title: 学生管理系统
tags: [MySQL, Java]
categories: [项目记录]
date: 2025-12-17
cover: img/屏幕截图 2025-12-17 233100.png
description: 基于springboot的学生管理系统，实现了简单的增删查改功能；
---

## 1. 前言 

### 1.1 所用技术栈
| 后端 | Springboot+Java21 | | 前端 | HTML+CSS+JavaScript | | 数据库 | MySQL |  | 构建工具 | Maven |

### 1.2 实现功能
- ✅ 添加学生信息
- ✅ 查看学生列表
- ✅ 编辑学生信息
- ✅ 删除学生（带确认）
- ✅ 实时搜索过滤
- ✅ 统计信息展示
- ✅ 响应式设计


## 2. 正文内容

### 2.1 DatabaseConnector.java
这个类是整个系统与数据库通信的"桥梁"，负责建立JDBC连接。
- 首先导入三个包：Connection负责与数据库对接和后续SQL操作；DriverManager用于加载数据库驱动并通过getConnection()方法创建与数据库的连接；SQLException负责抛出SQL异常。
- 接着先定义数据库的URL，用户名和密码，用于连接数据库；定义getConnection方法用于获取数据库连接对象。
- 使用Class.forName显式加载 MySQL 驱动程序；加载驱动后，DriverManager才能识别`jdbc:mysql://`协议（jdbc协议代表使用MySQL驱动）；最后调用DriverManager的静态方法建立数据库连接（一个Connection对象，用于执行SQL语句），这样就顺利连接到了数据库。
- 代码中使用了try-with-resources 语法，它可以在代码执行结束后自动关闭数据库连接，避免资源泄漏；而throws SQLException 将异常抛出，交由调用者统一处理。

### 2.1.1 代码演示
```java
package com.studentmanagement;      //DatabaseConnector.java

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

//封装管理数据库的连接
public class DatabaseConnector {

    private static final String URL = "jdbc:mysql://localhost:3306/school";
    private static final String USER = "root";
    private static final String PASSWORD = "123456";

    public static Connection getConnection() throws SQLException {  //负责连接到MySQL数据库
        try {
            // 加载 MySQL 驱动程序
            Class.forName("com.mysql.cj.jdbc.Driver");
            // 返回数据库连接（一个Connection对象，用于执行SQL语句）
            return DriverManager.getConnection(
                    "jdbc:mysql://localhost:3306/school",
                    "root",
                    "123456"
            );
        } catch (ClassNotFoundException e) {
            throw new SQLException("MySQL driver not found", e);
        }
    }
}

//java.sql.Connection包，用于创建数据库连接，后期创建PreparedStatement的对象执行来SQL命令
//java.sql.DriverManager包，用于加载MySQL驱动，提供getConnection方法连接数据库
//java.sql.SQLException包，用于当数据库操作错误时抛出数据库的异常
```

### 2.2 Student.java
这个类主要用于封装学生的信息，以及增删查改等具体数据库操作方法。
- 首先导入三个包：java.sql包下所有类（Statement, PreparedStatement, ResultSet等），用于JDBC操作；导入ArrayList，用于存储学生列表；导入List接口，用于定义返回类型；
- 定义Student类，用于表示学生实体及其数据库操作；定义学生实体的四个属性；定义有参构造方法，用于创建Student对象实例。
- 定义Getter方法，这些getter方法会在数据库操作中使用；重写Object类的toString方法，方便调试时打印对象信息。
- 最后分别定义增删查改方法。
- 特别注意在“增添学生”这个方法中，定义query字符串只有三个占位符，因为id在建表时设置为了自增主键，我们不需要再去设置id，按顺序设置SQL占位符的值。索引从1开始（不是0）。`executeUpdate()`用于执行INSERT/UPDATE/DELETE语句，返回受影响的行数。
- 在“查询学生”这个方法中，首先用List存储查询结果，初始为空，后面的ArrayList代表具体实现；`ResultSet`为结果集对象，像一个指针，可以逐行遍历查询结果；后面的while语句遍历结果集，将每一行数据转换为Student对象，添加到列表中。


### 2.2.1 代码演示
```java
package com.studentmanagement;      //Student.java

import java.sql.*;
import java.util.ArrayList;
import java.util.List;

//用于封装学生信息和数据库交互功能
public class Student {
    private int id;
    private String name;
    private int age;
    private String major;

    //用于创建学生对象实例（有参构造方法）
    public Student(int id, String name, int age, String major) {
        this.id = id;
        this.name = name;
        this.age = age;
        this.major = major;
    }

    //用于获取学生对象属性值
    public int getId() { return id; }
    public String getName() { return name; }
    public int getAge() { return age; }
    public String getMajor() { return major; }

    //重写toString方便打印学生对象信息
    @Override
    public String toString() {
        return String.format("Student[id=%d, name='%s', age=%d, major='%s']", id, name, age, major);
    }

    //增
    public static void insertStudent(Connection connection, Student student) throws SQLException {
        String query = "INSERT INTO students (name, age, major) VALUES (?, ?, ?)";
        try (PreparedStatement ps = connection.prepareStatement(query)) {
            ps.setString(1, student.getName());
            ps.setInt(2, student.getAge());
            ps.setString(3, student.getMajor());
            ps.executeUpdate(); //执行插入
        }
    }
    //删
    public static boolean deleteStudentById(Connection connection, int id) throws SQLException {
        String query = "DELETE FROM students WHERE id = ?";     //根据id删除学生
        try (PreparedStatement ps = connection.prepareStatement(query)) {
            ps.setInt(1, id);
            return ps.executeUpdate() > 0;      //若删除成功，返回true
        }
    }
    //查
    public static List<Student> getAllStudents(Connection connection) throws SQLException {
        List<Student> students = new ArrayList<>();     //创建一个学生列表
        String query = "SELECT id, name, age, major FROM students";
        try (PreparedStatement ps = connection.prepareStatement(query); ResultSet rs = ps.executeQuery()) { //用ResultSet来遍历数据库返回的记录
            while (rs.next()) {             //把数据库中查询到的信息转化为Student对象，加入列表
                students.add(new Student(
                        rs.getInt("id"),
                        rs.getString("name"),
                        rs.getInt("age"),
                        rs.getString("major")
                ));
            }
        }
        return students;        //返回Student对象（包含所有学生信息）
    }
    //改
    public static boolean updateStudent(Connection connection, Student student) throws SQLException {
        String query = "UPDATE students SET name = ?, age = ?, major = ? WHERE id = ?";     //根据id更新学生
        try (PreparedStatement ps = connection.prepareStatement(query)) {
            ps.setString(1, student.getName());
            ps.setInt(2, student.getAge());
            ps.setString(3, student.getMajor());
            ps.setInt(4, student.getId());
            return ps.executeUpdate() > 0;      //更新成功返回true
        }
    }
}

```


### 2.3 StudentController.java
这是整个项目最核心的类，作为**REST API控制器**，连接前端和数据库。

- 首先导入几个包，
- 注解@RestController，等于 `@Controller` + `@ResponseBody`两个注解组合；`@Controller`：标记这是一个Spring MVC控制器，会被Spring容器管理，`@ResponseBody`：表示方法返回值直接作为HTTP响应体（自动转JSON），而不是视图名。
- 注解@RequestMapping("/api/students")，定义这个控制器的**基础URL路径**，该控制器所有方法的URL都以 `/api/students` 开头，例如：GET `/api/students` 获取所有学生。
- 注解@CrossOrigin(origins = "*")，解决**跨域问题**，因为前端服务器和后端服务器可能在不同的端口，浏览器默认会阻止，`origins = "*"` 表示允许来自任何域的请求访问，可以解决这个问题。
- 注解@GetMapping，标记这个方法处理 **HTTP GET 请求**，相当于 `@RequestMapping(method = RequestMethod.GET)`；
- 其余的如@PostMapping注解，类似的表示标记这个方法处理 **HTTP GET 请求**；以添加学生为例，当用户在前端页面点击“添加学生”按钮，前端由JS事件监听器'submit'定位到handleFormSubmit方法，接着验证表单，由addStudent接受StudentData数据，发送POST请求给后端；请求到达服务器后，Spring Boot框架会进行路由匹配：由注解@RestController找到RESTAPI控制器；由注解@RequestMapping让spring知道所有以这个URL路径开头的请求都由这个类处理；因为请求是 POST /api/students，Spring会匹配到@PostMapping，由这个addStudent方法参数验证没问题后，会通过Connection conn = DatabaseConnector.getConnection()获取数据库连接；接着创建Studnet对象，调用Studnet类的静态方法将学生插入；最后调用辅助方法createSuccessResponse()创建响应体，Spring会自动把Map序列化为JSON,最后由前端处理响应，返回给客户端。


### 2.3.1 代码演示

```java
package com.studentmanagement.controller;           //StudentController.java

import com.studentmanagement.Student;
import com.studentmanagement.DatabaseConnector;
import org.springframework.http.ResponseEntity;             //这个包用于构建HTTP响应
import org.springframework.web.bind.annotation.*;           //这个包包含了处理HTTP请求的注解，如RestController, GetMapping等

import java.sql.Connection;
import java.sql.SQLException;
import java.util.HashMap;                                   //这个包用于创建HashMap数据结构
import java.util.List;                                      //这个包用于处理List数据结构
import java.util.Map;                                       //这个包用于处理Map数据结构

/**
 * 学生管理 REST API 控制器
 * 提供学生的增删改查接口
 */
@RestController                             //RestController注解表示这是一个控制器类，负责处理HTTP请求并返回响应体
@RequestMapping("/api/students")        //requestMapping注解定义了该控制器处理的基础URL路径
@CrossOrigin(origins = "*")               //允许跨域请求，允许来自任何来源的请求访问该控制器的接口
public class StudentController {         //StudentController类，处理与学生相关的HTTP请求

    /**
     * 获取所有学生列表
     * GET /api/students
     */
    @GetMapping                                                           //GetMapping注解表示该方法处理HTTP GET请求
    public ResponseEntity<?> getAllStudents() {                             //ResponseEntity<?>表示返回任意类型的响应体,?表示响应体可以是任意类型
        try (Connection conn = DatabaseConnector.getConnection()) {        //这里使用try-with-resources语法，确保数据库连接在使用后自动关闭
            List<Student> students = Student.getAllStudents(conn);        //调用Student类的静态方法getAllStudents获取所有学生列表
            return ResponseEntity.ok(students);                          //返回200 OK响应，响应体是学生列表
        } catch (SQLException e) {
            return ResponseEntity.status(500).body(createErrorResponse("获取学生列表失败：" + e.getMessage()));      //发生异常时，返回500错误响应，响应体包含错误信息
        }
    }

    /**
     * 添加新学生
     * POST /api/students
     */
    @PostMapping                                                                                       //PostMapping注解表示该方法处理HTTP POST请求
    public ResponseEntity<?> addStudent(@RequestBody StudentRequest request) {                           //@RequestBody注解表示请求体会被反序列化为StudentRequest对象
        // 参数验证
        if (request.getName() == null || request.getName().trim().isEmpty()) {                           //如果姓名为空，返回400错误响应
            return ResponseEntity.badRequest().body(createErrorResponse("姓名不能为空"));                  //返回错误响应体
        }
        if (request.getAge() == null || request.getAge() <= 0) {                                        //如果年龄不是正整数，返回400错误响应
            return ResponseEntity.badRequest().body(createErrorResponse("年龄必须是正整数"));              //返回错误响应体
        }
        if (request.getMajor() == null || request.getMajor().trim().isEmpty()) {                       //如果专业为空，返回400错误响应
            return ResponseEntity.badRequest().body(createErrorResponse("专业不能为空"));                //返回错误响应体
        }

        try (Connection conn = DatabaseConnector.getConnection()) {                                                        //这里使用try-with-resources语法，确保数据库连接在使用后自动关闭
            Student student = new Student(0, request.getName().trim(), request.getAge(), request.getMajor().trim());   //创建新的学生对象
            Student.insertStudent(conn, student);                                                                        //调用Student类的静态方法insertStudent将学生添加到数据库
            return ResponseEntity.ok(createSuccessResponse("学生添加成功"));                                               //返回200 OK响应，响应体包含成功信息
        } catch (SQLException e) {
            return ResponseEntity.status(500).body(createErrorResponse("添加学生失败：" + e.getMessage()));               //返回500错误响应，响应体包含错误信息
        }
    }

    /**
     * 更新学生信息
     * PUT /api/students/{id}
     */
    @PutMapping("/{id}")                                                                                    //PutMapping注解表示该方法处理HTTP PUT请求，{id}是路径变量
    public ResponseEntity<?> updateStudent(@PathVariable int id, @RequestBody StudentRequest request) {       //@PathVariable注解表示从URL路径中提取id参数，@RequestBody注解表示请求体会被反序列化为StudentRequest对象
        // 参数验证
        if (request.getName() == null || request.getName().trim().isEmpty()) {                              //如果姓名为空，返回400错误响应
            return ResponseEntity.badRequest().body(createErrorResponse("姓名不能为空"));
        }
        if (request.getAge() == null || request.getAge() <= 0) {                                           //如果年龄不是正整数，返回400错误响应
            return ResponseEntity.badRequest().body(createErrorResponse("年龄必须是正整数"));
        }
        if (request.getMajor() == null || request.getMajor().trim().isEmpty()) {                          //如果专业为空，返回400错误响应
            return ResponseEntity.badRequest().body(createErrorResponse("专业不能为空"));
        }

        try (Connection conn = DatabaseConnector.getConnection()) {                                      //这里使用try-with-resources语法，确保数据库连接在使用后自动关闭
            Student student = new Student(id, request.getName().trim(), request.getAge(), request.getMajor().trim());
            boolean success = Student.updateStudent(conn, student);                                     //调用Student类的静态方法updateStudent更新学生信息
            if (success) {
                return ResponseEntity.ok(createSuccessResponse("学生信息更新成功"));                       //返回200 OK响应，响应体包含成功信息
            } else {
                return ResponseEntity.status(404).body(createErrorResponse("未找到该学生"));              //返回404错误响应，响应体包含错误信息
            }
        } catch (SQLException e) {
            return ResponseEntity.status(500).body(createErrorResponse("更新学生失败：" + e.getMessage()));    //返回500错误响应，响应体包含错误信息
        }
    }

    /**
     * 删除学生
     * DELETE /api/students/{id}
     */
    @DeleteMapping("/{id}")                                                                                //DeleteMapping注解表示该方法处理HTTP DELETE请求，{id}是路径变量
    public ResponseEntity<?> deleteStudent(@PathVariable int id) {                                           //@PathVariable注解表示从URL路径中提取id参数
        try (Connection conn = DatabaseConnector.getConnection()) {                                         //这里使用try-with-resources语法，确保数据库连接在使用后自动关闭
            boolean success = Student.deleteStudentById(conn, id);
            if (success) {
                return ResponseEntity.ok(createSuccessResponse("学生删除成功"));                              //返回200 OK响应，响应体包含成功信息
            } else {
                return ResponseEntity.status(404).body(createErrorResponse("未找到该学生"));                  //返回404错误响应，响应体包含错误信息
            }
        } catch (SQLException e) {
            return ResponseEntity.status(500).body(createErrorResponse("删除学生失败：" + e.getMessage()));   //返回500错误响应，响应体包含错误信息
        }
    }

    /**
     * 创建成功响应
     */
    private Map<String, Object> createSuccessResponse(String message) {             //创建一个Map作为响应体，包含success和message字段
        Map<String, Object> response = new HashMap<>();                             //创建一个HashMap实例
        response.put("success", true);                                              //设置success字段为true
        response.put("message", message);                                           //设置message字段为传入的消息
        return response;                                                            //返回响应体
    }

    /**
     * 创建错误响应
     */
    private Map<String, Object> createErrorResponse(String message) {               //创建一个Map作为响应体，包含success和message字段
        Map<String, Object> response = new HashMap<>();
        response.put("success", false);                                             //设置success字段为false
        response.put("message", message);                                           //设置message字段为传入的消息
        return response;
    }

    /**
     * 学生请求体类
     */
    public static class StudentRequest {                                            //StudentRequest类，表示添加或更新学生时的请求体
        private String name;
        private Integer age;
        private String major;

        public String getName() { return name; }                                    //这个方法用于获取name属性值
        public void setName(String name) { this.name = name; }                      //这个方法用于设置name属性值
        public Integer getAge() { return age; }
        public void setAge(Integer age) { this.age = age; }
        public String getMajor() { return major; }
        public void setMajor(String major) { this.major = major; }
    }
}

```

### 2.4 JavaScript代码
- 主要负责前端的交互功能


### 2.4.1 JavaScript代码

```JavaScript
/**
 * 学生管理系统 - 前端 JavaScript
 * 实现与后端 API 的交互和页面动态功能
 */

//定义后端API的基础路径，所有请求都基于这个路径
const API_BASE_URL = '/api/students';

// DOM 元素引用，获取页面上的HTML元素引用，用于后续操作
const studentForm = document.getElementById('studentForm');
const studentTableBody = document.getElementById('studentTableBody');
const formTitle = document.getElementById('formTitle');
const submitBtn = document.getElementById('submitBtn');
const cancelBtn = document.getElementById('cancelBtn');
const refreshBtn = document.getElementById('refreshBtn');
const searchInput = document.getElementById('searchInput');
const emptyState = document.getElementById('emptyState');
const studentTable = document.getElementById('studentTable');
const toast = document.getElementById('toast');
const toastMessage = document.getElementById('toastMessage');
const confirmModal = document.getElementById('confirmModal');
const confirmDelete = document.getElementById('confirmDelete');
const cancelDelete = document.getElementById('cancelDelete');

// 输入字段
const studentIdField = document.getElementById('studentId');
const nameField = document.getElementById('name');
const ageField = document.getElementById('age');
const majorField = document.getElementById('major');

// 统计元素
const totalCountEl = document.getElementById('totalCount');
const avgAgeEl = document.getElementById('avgAge');
const majorCountEl = document.getElementById('majorCount');

// 存储所有学生数据（用于搜索过滤）
let allStudents = [];

// 当前要删除的学生 ID
let deleteStudentId = null;

// ==================== 初始化 ====================

document.addEventListener('DOMContentLoaded', function() {      // DOMContentLoaded表示在DOM内容加载完成后执行
    // 页面加载完成后加载学生列表
    loadStudents();

    // 绑定事件监听器（为各个元素绑定事件处理函数）
    studentForm.addEventListener('submit', handleFormSubmit);       // 表单提交事件
    cancelBtn.addEventListener('click', resetForm);                 // 取消编辑按钮事件
    refreshBtn.addEventListener('click', loadStudents);             // 刷新按钮事件
    searchInput.addEventListener('input', handleSearch);            // 搜索输入事件
    confirmDelete.addEventListener('click', handleConfirmDelete);   // 确认删除按钮事件
    cancelDelete.addEventListener('click', closeConfirmModal);      // 取消删除按钮事件

    // 点击模态框外部关闭
    confirmModal.addEventListener('click', function(e) {
        if (e.target === confirmModal) {
            closeConfirmModal();
        }
    });
});

// ==================== API 请求函数 ====================

/**
 * 获取所有学生
 */
async function loadStudents() {
    try {
        const response = await fetch(API_BASE_URL);            // 使用fetch API发送GET请求获取学生数据（fetch现代浏览器提供的HTTP请求API）
        if (!response.ok) {
            throw new Error('获取学生列表失败');
        }
        allStudents = await response.json();                   // 解析响应为JSON格式
        renderStudentTable(allStudents);                       // 渲染学生表格
        updateStatistics(allStudents);                         // 更新统计信息
    } catch (error) {
        console.error('Error:', error);
        showToast('加载学生列表失败：' + error.message, 'error');
    }
}

/**
 * 添加学生
 */
async function addStudent(studentData) {
    try {
        const response = await fetch(API_BASE_URL, {        // 发送POST请求添加新学生
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'          // 设置请求头，表示发送JSON数据
            },
            body: JSON.stringify(studentData)               // 将学生数据转换为JSON字符串作为请求体
        });

        const result = await response.json();               // 解析响应为JSON格式

        if (response.ok && result.success) {                // 检查响应状态和结果
            showToast('学生添加成功！', 'success');
            resetForm();                                    // 重置表单
            loadStudents();                                 // 重新加载学生列表
        } else {
            showToast(result.message || '添加失败', 'error');
        }
    } catch (error) {
        console.error('Error:', error);
        showToast('添加学生失败：' + error.message, 'error');
    }
}

/**
 * 更新学生
 */
async function updateStudent(id, studentData) {
    try {
        const response = await fetch(`${API_BASE_URL}/${id}`, {       // 发送PUT请求更新指定ID学生信息
            method: 'PUT',
            headers: {
                'Content-Type': 'application/json'                    // 设置请求头，表示发送JSON数据
            },
            body: JSON.stringify(studentData)                         // 将学生数据转换为JSON字符串作为请求体
        });

        const result = await response.json();                         // 解析响应为JSON格式

        if (response.ok && result.success) {                          // 检查响应状态和结果
            showToast('学生信息更新成功！', 'success');
            resetForm();
            loadStudents();
        } else {
            showToast(result.message || '更新失败', 'error');
        }
    } catch (error) {
        console.error('Error:', error);
        showToast('更新学生失败：' + error.message, 'error');
    }
}

/**
 * 删除学生
 */
async function deleteStudent(id) {
    try {
        const response = await fetch(`${API_BASE_URL}/${id}`, {     // 发送DELETE请求删除指定ID学生
            method: 'DELETE'
        });

        const result = await response.json();                       // 解析响应为JSON格式

        if (response.ok && result.success) {                        // 检查响应状态和结果
            showToast('学生删除成功！', 'success');
            loadStudents();
        } else {
            showToast(result.message || '删除失败', 'error');
        }
    } catch (error) {
        console.error('Error:', error);
        showToast('删除学生失败：' + error.message, 'error');
    }
}

// ==================== 表格渲染 ====================

/**
 * 渲染学生表格
 */
function renderStudentTable(students) {
    studentTableBody.innerHTML = '';

    if (students.length === 0) {
        studentTable.style.display = 'none';
        emptyState.style.display = 'block';
        return;
    }

    studentTable.style.display = 'table';
    emptyState.style.display = 'none';

    students.forEach(student => {
        const row = document.createElement('tr');
        row.innerHTML = `
            <td>${student.id}</td>
            <td>${escapeHtml(student.name)}</td>
            <td>${student.age}</td>
            <td>${escapeHtml(student.major)}</td>
            <td>
                <div class="action-btns">
                    <button class="btn btn-sm btn-success" onclick="editStudent(${student.id})">
                        ✏️ 编辑
                    </button>
                    <button class="btn btn-sm btn-danger" onclick="showDeleteConfirm(${student.id}, '${escapeHtml(student.name)}')">
                        🗑️ 删除
                    </button>
                </div>
            </td>
        `;
        studentTableBody.appendChild(row);
    });
}

// ==================== 表单处理 ====================

/**
 * 处理表单提交
 */
function handleFormSubmit(e) {
    e.preventDefault();                 // 阻止默认表单提交行为

    const studentData = {
        name: nameField.value.trim(),       // 获取并修剪输入值
        age: parseInt(ageField.value),      // 将年龄转换为整数
        major: majorField.value.trim()      // 获取并修剪输入值
    };

    // 验证
    if (!studentData.name) {
        showToast('请输入学生姓名', 'warning');
        nameField.focus();
        return;
    }

    if (!studentData.age || studentData.age <= 0) {
        showToast('请输入有效的年龄', 'warning');
        ageField.focus();
        return;
    }

    if (!studentData.major) {
        showToast('请输入专业', 'warning');
        majorField.focus();
        return;
    }

    const studentId = studentIdField.value;

    if (studentId) {
        // 更新模式
        updateStudent(parseInt(studentId), studentData);    // 有ID则为更新模式
    } else {
        // 添加模式
        addStudent(studentData);                        // 无ID则为添加模式
    }
}

/**
 * 编辑学生 - 填充表单
 */
function editStudent(id) {
    const student = allStudents.find(s => s.id === id);     // 从缓存中查找学生
    if (!student) {
        showToast('未找到学生信息', 'error');                  // 学生不存在则显示错误
        return;
    }

    // 填充表单
    studentIdField.value = student.id;              // 设置隐藏的学生ID字段
    nameField.value = student.name;
    ageField.value = student.age;
    majorField.value = student.major;

    // 切换为编辑模式
    formTitle.textContent = '编辑学生';
    submitBtn.innerHTML = '<span class="btn-icon">✓</span> 保存修改';
    cancelBtn.style.display = 'inline-flex';

    // 滚动到表单位置
    document.querySelector('.form-section').scrollIntoView({ behavior: 'smooth' });
    nameField.focus();
}

/**
 * 重置表单
 */
function resetForm() {
    studentForm.reset();
    studentIdField.value = '';
    formTitle.textContent = '添加学生';
    submitBtn.innerHTML = '<span class="btn-icon">+</span> 添加学生';
    cancelBtn.style.display = 'none';
}

// ==================== 删除确认 ====================

/**
 * 显示删除确认对话框
 */
function showDeleteConfirm(id, name) {
    deleteStudentId = id;
    document.getElementById('confirmMessage').textContent =
        `确定要删除学生 "${name}" 吗？此操作不可恢复。`;
    confirmModal.classList.add('show');
}

/**
 * 关闭删除确认对话框
 */
function closeConfirmModal() {
    confirmModal.classList.remove('show');
    deleteStudentId = null;
}

/**
 * 确认删除
 */
function handleConfirmDelete() {
    if (deleteStudentId) {
        deleteStudent(deleteStudentId);
        closeConfirmModal();
    }
}

// ==================== 搜索功能 ====================

/**
 * 处理搜索
 */
function handleSearch() {
    const keyword = searchInput.value.trim().toLowerCase();     // 获取搜索关键词

    if (!keyword) {
        renderStudentTable(allStudents);                        // 关键词为空则显示全部
        return;
    }

    const filteredStudents = allStudents.filter(student =>      // 过滤符合条件的学生
        student.name.toLowerCase().includes(keyword) ||         // 姓名匹配
        student.major.toLowerCase().includes(keyword) ||        // 专业匹配
        student.id.toString().includes(keyword)                 // ID匹配
    );

    renderStudentTable(filteredStudents);                       // 渲染过滤后的结果
}

// ==================== 统计功能 ====================

/**
 * 更新统计信息
 */
function updateStatistics(students) {
    // 学生总数
    totalCountEl.textContent = students.length;

    // 平均年龄
    if (students.length > 0) {
        const totalAge = students.reduce((sum, s) => sum + s.age, 0);
        avgAgeEl.textContent = (totalAge / students.length).toFixed(1);
    } else {
        avgAgeEl.textContent = '0';
    }

    // 专业数量
    const majors = new Set(students.map(s => s.major));
    majorCountEl.textContent = majors.size;
}

// ==================== 工具函数 ====================

/**
 * 显示消息提示
 */
function showToast(message, type = 'info') {
    toastMessage.textContent = message;
    toast.className = 'toast';
    toast.classList.add(type);
    toast.classList.add('show');

    setTimeout(() => {
        toast.classList.remove('show');
    }, 3000);
}

/**
 * HTML 转义，防止 XSS 攻击
 */
function escapeHtml(text) {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}

```


## 3. 总结
我学到了什么？

### 关于JDBC
- 详细总结Connection类在JDBC中扮演什么角色？
Connection是JDBC中最核心的接口之一，它代表Java程序与数据库之间的一条通信通道。(StudentController类 第31行，conn就是“通道”，通过conn对象，Java就可以向数据库发送SQL并获取结果)

Connection的主要职责：
1. **建立连接** -> 与数据库服务器建立TCP连接 -> DriverManager.getConnection()
2. **创建语句对象** -> 生成执行SQL的Statement或PreparedStatement -> conn.prepareStatement(sql)
3. **事务管理** -> 控制事务的提交和回滚 -> setAutoCommit(), commit(), rollback()
4. **资源释放** -> 关闭连接，释放数据库资源 -> close()


- getConnection()方法在JDBC中扮演什么角色？
getConnection()是获取数据库连接的入口方法，它负责根据提供的信息建立与数据库的连接。

**工作流程**：（DatabaseConnector类 14行）
getConnection(url, user, password)
        │
        ▼
┌───────────────────────────┐
│  1. 解析URL，确定数据库类型  │
│  2. 查找对应的JDBC驱动     │
│  3. 通过驱动建立TCP连接     │
│  4. 完成身份验证           │
│  5. 返回Connection对象     │
└───────────────────────────┘
        │
        ▼
   Connection对象


- 详细总结DriverManager类：在JDBC中扮演什么角色？
DriverManager是JDBC的驱动管理器，它就像一个"中介"或"调度中心"。

┌─────────────────────────────────────────────────────────┐
│                    DriverManager                         │
│                     (驱动管理器)                          │
├─────────────────────────────────────────────────────────┤
│  已注册驱动列表：                                         │
│  ├── MySQL Driver (com.mysql.cj.jdbc.Driver)            │
│  ├── Oracle Driver                                       │
│  └── PostgreSQL Driver                                   │
└─────────────────────────────────────────────────────────┘
         │
         │ getConnection(url, user, password)
         ▼
   根据URL前缀选择合适的驱动，建立连接

1. **驱动注册** 维护所有已加载的JDBC驱动列表
2. **驱动选择** 根据URL自动选择匹配的驱动
3. **连接分发** 调用选中驱动的connect()方法获取连接


- PreparedStatement和Statement的区别？
**Statement**：SQL编写用拼接字符串；SQL注入有风险；每次都要重新编译；可读性差；
**PreparedStatement**：SQL编写使用?占位符；SQL注入安全；预编译，可复用；可读性好；


- 详细总结ResultSet是怎么遍历和存储结果的？
ResultSet是查询结果的容器，它像一个"游标"指向结果集。

┌─────────────────────────────────────────┐
│              ResultSet结构               │
├─────────────────────────────────────────┤
│  (游标初始位置 - 在第一行之前)            │
├────┬────────┬─────┬──────────┬──────────┤
│ id │  name  │ age │  major   │          │
├────┼────────┼─────┼──────────┼──────────┤
│ 1  │ 张三   │  20 │ 软件工程  │ ◄── rs.next() 第1次
├────┼────────┼─────┼──────────┼──────────┤
│ 2  │ 李四   │  21 │ 计算机   │ ◄── rs.next() 第2次
├────┼────────┼─────┼──────────┼──────────┤
│ 3  │ 王五   │  22 │ 数据科学  │ ◄── rs.next() 第3次
└────┴────────┴─────┴──────────┴──────────┘
                                    │
                         rs.next() 返回false，遍历结束


1. rs.next() - 移动游标到下一行，返回是否有数据
2. rs.getXxx("列名") - 获取当前行指定列的值
3. ResultSet不是把所有数据一次性加载，而是按需从数据库获取


### 关于controller
- StudentController类调用getConnection()方法的过程是怎么样的？

┌─────────────────────────────────────────────────────────────────┐
│                    完整调用链路                                  │
└─────────────────────────────────────────────────────────────────┘

StudentController.java (第31行)
│
│  try (Connection conn = DatabaseConnector.getConnection())
│                                    │
                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  DatabaseConnector.java - getConnection()方法                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ public static Connection getConnection() throws SQLException│ │
│  │     return DriverManager.getConnection(URL, USER, PASSWORD);│ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  DriverManager（JDK内置类）                                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. 解析URL: jdbc:mysql://localhost:3306/student_management │ │
│  │ 2. 识别协议: mysql                                          │ │
│  │ 3. 查找MySQL驱动: com.mysql.cj.jdbc.Driver                  │ │
│  │ 4. 调用驱动的connect()方法                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  MySQL JDBC Driver（mysql-connector-j.jar）                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1. 建立TCP连接到 localhost:3306                             │ │
│  │ 2. 发送MySQL握手协议                                        │ │
│  │ 3. 发送用户名密码进行认证                                    │ │
│  │ 4. 选择数据库: student_management                           │ │
│  │ 5. 创建并返回Connection对象                                 │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
                          返回Connection对象给Controller


- 到底什么是RestAPI控制器？它在项目中有什么作用？
REST API控制器 = 接收HTTP请求 + 处理业务 + 返回JSON响应

┌────────────┐     HTTP请求      ┌──────────────────┐
│   前端      │ ───────────────► │  REST API控制器   │
│  (浏览器)   │                  │ StudentController │
│            │ ◄─────────────── │                   │
└────────────┘     JSON响应      └──────────────────┘

**REST是一种设计风格**：
HTTP方法 -> 对应操作 -> 我项目中的URL -> 对应方法
GET -> 查询 -> api/students -> getAllStudents()
POST -> 新增 -> /api/students -> addStudent()
PUT -> 修改 -> /api/students/{id} -> updateStudent()
DELETE -> 删除 -> /api/students/{id} -> deleteStudent()


- 我还是不懂springboot在项目中有什么用，给我详细总结一下。
Spring Boot = 简化配置 + 自动装配 + 内嵌服务器

┌─────────────────────────────────────────────────────────────────┐
│                    没有Spring Boot的时代                         │
├─────────────────────────────────────────────────────────────────┤
│  1. 手动下载Tomcat服务器                                         │
│  2. 编写大量XML配置文件                                          │
│  3. 手动配置数据源、事务管理器、视图解析器...                      │
│  4. 打包成WAR，部署到Tomcat                                      │
│  5. 依赖冲突要自己解决                                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼ Spring Boot出现后
┌─────────────────────────────────────────────────────────────────┐
│                    有Spring Boot的现在                           │
├─────────────────────────────────────────────────────────────────┤
│  1. 内嵌Tomcat，直接运行main方法                                  │
│  2. 约定大于配置，几乎不用写XML                                   │
│  3. 自动配置大部分组件                                           │
│  4. 打包成JAR，java -jar直接运行                                 │
│  5. starter帮你管理依赖版本                                      │
└─────────────────────────────────────────────────────────────────┘

**Spring Boot为我项目做了什么**：
功能 -> 说明 -> 我需要做什么
内嵌Tomcat -> 不用安装服务器 -> 直接运行main方法
自动扫描 -> 发现@RestController等注解 -> 把类放在正确包下
JSON转换 -> 自动将对象转JSON -> 引入jackson依赖（starter自带）
端口配置 -> 默认8080 -> 可在application.properties修改
静态资源 -> 自动映射static目录 -> 把HTML/CSS/JS放进去就行


- springboot是不是会根据我写的注解来理解我的意图？它会自动帮我处理哪些内容？

我的代码：
```java
@PostMapping
public ResponseEntity<?> addStudent(@RequestBody StudentRequest request) {
    // ...
}

```
// Spring Boot自动帮我做的事情：

┌─────────────────────────────────────────────────────────────────┐
│                 Spring Boot自动处理流程                          │
└─────────────────────────────────────────────────────────────────┘

前端发送: POST /api/students
         Content-Type: application/json
         {"name":"张三","age":20,"major":"软件工程"}
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 1. URL匹配                                                       │
│    @RequestMapping("/api/students") + @PostMapping               │
│    → 找到addStudent方法                                          │
├─────────────────────────────────────────────────────────────────┤
│ 2. 参数解析 (@RequestBody)                                       │
│    → 读取请求体的JSON                                            │
│    → 使用Jackson库反序列化                                       │
│    → 创建StudentRequest对象                                      │
│    → 调用setName("张三"), setAge(20), setMajor("软件工程")        │
├─────────────────────────────────────────────────────────────────┤
│ 3. 方法调用                                                      │
│    → 把填充好的request对象传入addStudent方法                      │
├─────────────────────────────────────────────────────────────────┤
│ 4. 响应处理                                                      │
│    → 你返回ResponseEntity.ok(map)                                │
│    → Spring自动将map序列化为JSON                                 │
│    → 设置Content-Type: application/json                         │
│    → 设置状态码200                                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
前端收到: HTTP 200 OK
         {"success":true,"message":"学生添加成功"}

各注解的自动处理：
注解 -> Spring Boot自动做的事
@RestController -> 注册为控制器Bean，返回值自动转JSON
@RequestMapping -> 建立URL到控制器的映射
@GetMapping/@PostMapping -> 建立HTTP方法到具体方法的映射
@RequestBody -> 读取请求体，JSON→Java对象
@PathVariable -> 从URL路径提取参数
@CrossOrigin -> 添加CORS响应头

### 下次要改进的地方

- 通过这个项目，你建议我重点复习或学习哪些内容？（我是Javaweb初学者）

┌─────────────────────────────────────────────────────────────────┐
│                    建议复习的知识点                               │
└─────────────────────────────────────────────────────────────────┘

【基础层 - 必须掌握】
├── JDBC核心概念
│   ├── Connection, PreparedStatement, ResultSet的生命周期
│   ├── SQL注入原理与防护
│   └── 事务的概念（虽然你项目用的是自动提交）
│
├── HTTP协议基础
│   ├── 请求方法: GET/POST/PUT/DELETE
│   ├── 状态码: 200/400/404/500
│   ├── 请求头/响应头
│   └── JSON数据格式
│
└── Java基础
    ├── 异常处理 try-catch-finally, try-with-resources
    ├── 集合框架 List, Map
    └── 面向对象：封装、静态方法

【框架层 - 理解原理】
├── Spring核心概念
│   ├── IoC（控制反转）和DI（依赖注入）
│   ├── Bean的概念
│   └── 注解驱动开发
│
└── MVC架构模式
    ├── Model-View-Controller分层思想
    └── 前后端分离架构


- 基于我目前的代码和框架，你建议我下一步如何升级项目？比如前端用vue，或者用什么代替JDBC连接数据库？

┌─────────────────────────────────────────────────────────────────┐
│                      项目升级路线图                              │
└─────────────────────────────────────────────────────────────────┘

【第一阶段：后端优化】（建议先做）
┌─────────────────────────────────────────────────────────────────┐
│ 用 MyBatis 替代原生JDBC                                          │
│                                                                  │
│ 原生JDBC（你现在用的）:                                          │
│   String sql = "SELECT * FROM students";                        │
│   PreparedStatement ps = conn.prepareStatement(sql);            │
│   ResultSet rs = ps.executeQuery();                             │
│   while(rs.next()) { ... }                                      │
│                                                                  │
│ MyBatis:                                                         │
│   @Select("SELECT * FROM students")                             │
│   List<Student> getAllStudents();  // 一行搞定！                 │
└─────────────────────────────────────────────────────────────────┘

【第二阶段：前端升级】
┌─────────────────────────────────────────────────────────────────┐
│ 用 Vue.js 替代原生JavaScript                                     │
│                                                                  │
│ 原生JS（你现在用的）:                                            │
│   document.getElementById('xxx').innerHTML = ...                │
│   手动操作DOM，容易出错                                          │
│                                                                  │
│ Vue.js:                                                          │
│   <div>{{ student.name }}</div>                                 │
│   数据驱动，自动更新页面                                          │
└─────────────────────────────────────────────────────────────────┘

【第三阶段：功能扩展】
├── 添加用户登录功能（Spring Security）
├── 添加分页查询
├── 添加数据导出（Excel）
└── 部署到云服务器


- 告诉我后期的学习内容和路线。

┌─────────────────────────────────────────────────────────────────┐
│                  JavaWeb学习路线图                               │
│                  (基于你目前的水平)                              │
└─────────────────────────────────────────────────────────────────┘

你现在的位置 ★
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│ 【阶段1】巩固基础（1-2个月）                                      │
├─────────────────────────────────────────────────────────────────┤
│ □ MySQL进阶：索引、事务、锁                                      │
│ □ Java进阶：泛型、反射、注解原理                                  │
│ □ Maven：依赖管理、项目构建                                      │
│ □ Git：版本控制（必学！）                                        │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│ 【阶段2】框架深入（2-3个月）                                      │
├─────────────────────────────────────────────────────────────────┤
│ □ Spring Framework：IoC、AOP原理                                │
│ □ MyBatis：ORM映射、动态SQL                                      │
│ □ Spring Boot：自动配置原理                                      │
│ □ RESTful API设计规范                                           │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│ 【阶段3】前端技术（1-2个月）                                      │
├─────────────────────────────────────────────────────────────────┤
│ □ ES6语法                                                        │
│ □ Vue.js 3（推荐）或 React                                       │
│ □ 前端工程化：npm、webpack/vite                                  │
│ □ UI组件库：Element Plus                                        │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│ 【阶段4】企业级技术（2-3个月）                                    │
├─────────────────────────────────────────────────────────────────┤
│ □ Spring Security：认证授权                                      │
│ □ Redis：缓存                                                    │
│ □ 消息队列：RabbitMQ基础                                         │
│ □ Docker：容器化部署                                             │
│ □ Linux：基本命令                                                │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│ 【阶段5】实战项目                                                │
├─────────────────────────────────────────────────────────────────┤
│ □ 做一个完整的前后端分离项目                                      │
│ □ 推荐：电商系统 / 博客系统 / 在线教育平台                        │
│ □ 部署到云服务器                                                 │
│ □ 写好README，放到GitHub                                        │
└─────────────────────────────────────────────────────────────────┘

这个简单项目已经完整走通了Web开发的核心流程：
**前端发请求 → 后端处理 → 数据库操作 → 返回响应**

## 4. 参考资料
- [资料 1]()

- [资料 2]()
