---
title: '151.反转字符串中的单词'
tags: [Leetcode]
categories: 刷题笔记
leetcode_id: '151'
difficulty: '中等'
source: '代码随想录'
topics: [字符串] [双指针法]
languages: Java
solved: false
date: 2025-09-22 19:48:54
---

# 题目
- **题目 ID / 标题**：151.反转字符串中的单词
- **原题链接**：https://leetcode.cn/problems/reverse-words-in-a-string/

---

## 1. 题目理解（审题）
- 输入输出是什么？
  **输入**：字符串s，s由多个单词构成，单词之间至少有一个空格。但是字符串开头或者结尾也可以存在空格，单词之间也可以有多个空格。

  **输出**：顺序反转的字符串s，单词顺序完全相反，单词之间只可以有一个空格。

- 有哪些约束条件（N 的范围、是否有负数、是否有重复等）？

  重点是返回时只可以有一个空格，而s可能包含多个空格，这该怎么解决？

---

## 2. 解题思路总览
- 思路一（暴力 / 直观）：
  - 核心想法：
      遍历字符串s，以空格为分界符，依次**找到各个单词**，存放到一个**字符数组**，最后将字符数组的值从最后开始**反向输出**，输出一个值就添加一个空格。这里会遇到几个问题：
      - s开头就有空格
      - 单词之间有多个空格
      - s结尾有空格

- 思路二（优化）：
  - 核心想法：
  - 关键实现要点（如哈希表、双指针、滑动窗口、动规状态转移）：

---

## 3. 关键步骤与伪代码
- 写出伪代码或关键步骤，方便把思路转成代码：


// 核心步骤
function solve(input):
  // 1. **找到各个单词**：
  // 2. **存放字符数组**：
  // 3. **反向输出字符数组**：
  // 4. **多个空格问题**：
        

---

## 4. 代码实现（示例）
### Java
```java
class Solution {
    public String reverseWords(String s) {
        List<String> words = new ArrayList<>();

        int n = s.length();
        int i = 0;

        while (i < n) {
            while (i < n && s.charAt(i) == ' ') i++;
            if (i >= n) break;

            int start = i;

            while (i < n && s.charAt(i) != ' ') i++;

            words.add(s.substring(start, i));
        }
        
        StringBuilder sb = new StringBuilder();
        for (int idx = words.size() - 1; idx >= 0; idx--) {
            sb.append(words.get(idx));
            if (idx != 0) sb.append(' ');
        }
        return sb.toString();

    }
}
```

---

## 5. 复杂度分析
- 时间复杂度：O(N)
- 空间复杂度：O(N)

---

## 7. 总结 / 反思
- 写完重新梳理一下这道题：
  1. 题目求什么？
      给定字符串s，反转其中单词的顺序（倒序），字符串中可能有多个空格，但返回结果时，每个单词之间只可以有一个空格，也就是说题目要求：
      - 找个容器存储结果 => List<String> words = new ArrayList<>();
      - 找出字符串中单词 
      - 消除空格影响
      - 将单词反转
      - 拼装结果



- 还需要补哪些知识点？
  1. List<String>  关于泛型和String类
  2. s.charAt(i)，s.substring(start, i)， 关于String类的方法
  3. StringBuilder类，append()方法，我不清楚

---

## 8. 标签
- 题型：字符串
- 难度：中等 
- 来源：代码随想录


<!-- 使用方法：
1. 把此文件保存为 myblog/_scaffolds/note.md
2. 执行： hexo new note "题目标题" 
3. 在 source/_posts/ 下编辑生成的 md 文件即可 -->
